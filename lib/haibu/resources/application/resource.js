var resourceful = require('resourceful');
var Application = module.exports = resourceful.define(require('./schema.json'));

Application.prototype.getRepositories = function getRepository (version, callback) {
    
}
Application.prototype.getConfiguration = function getConfiguration (environment, callback) {
    
}